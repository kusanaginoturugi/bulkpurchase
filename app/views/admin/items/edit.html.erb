<%= render "admin/shared/header", title: "道具編集", description: "#{@item.code} #{@item.name}" %>
<section class="rounded-[2rem] border border-stone-200 bg-white p-6 shadow-sm">
  <%= render "form", item: @item %>
</section>
